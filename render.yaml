services:
  - type: web
    name: barcode-generator
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m waitress --listen=0.0.0.0:10000 app:app
